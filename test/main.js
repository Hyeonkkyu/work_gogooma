// window.addEventListener('load', function () {
//     this.addEventListener('scroll', function () {
//         let calcHeight = this.document.querySelector('header').scrollHeight + this.document.querySelector('main').scrollHeight;
//         let nowHeight = this.document.scrollingElement.scrollTop + screen.height;
//         if (nowHeight >= calcHeight) {
//             this.document.querySelectorAll('.btn').forEach(element => {
//                 element.style.position = "relative";
//                 element.style.bottom = "-95%";
//             });
//         } else {
//             this.document.querySelectorAll('.btn').forEach(element => {
//                 element.style.position = "fixed";
//                 element.style.bottom = "10px";
//             });
//         }
//     });
// });
